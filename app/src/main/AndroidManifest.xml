<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.plannerbeta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Plannerbeta">
        <activity android:name=".itensdalista"></activity>
        <activity android:name=".view.Andamento" />
        <activity android:name=".view.TodasTarefas" />
        <activity android:name=".view.Importante" />
        <activity android:name=".view.Concluido" />
        <activity android:name=".view.Nova_tarefa" />
        <activity android:name=".view.Telainicial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>